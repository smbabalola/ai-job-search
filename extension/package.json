{
  "name": "application-handoff-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser extension for the Application Handoff feature (Phase 2).",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build:test-bundle": "esbuild src/adapters/index.ts --bundle --format=iife --global-name=HandoffAdapters --outfile=dist/adapters-bundle.js",
    "build:probe-test-bundle": "esbuild src/content/probe.ts --bundle --format=iife --global-name=HandoffProbe --outfile=dist/probe-bundle.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "@types/jsdom": "^30.0.0",
    "esbuild": "^0.28.2",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
